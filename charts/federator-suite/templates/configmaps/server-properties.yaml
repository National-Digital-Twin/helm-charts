{{- if .Values.federatorServer.enabled }}
---
apiVersion: v1
kind: ConfigMap
metadata:
  name: federator-server-properties
  namespace: {{ .Release.Namespace }}
  labels:
    {{ include "federator-suite.server.labels" . | nindent 4 }}
data:
  server.properties: |-
{{ include "federator-suite.serverProperties" . | nindent 4 }}
{{- end }}
