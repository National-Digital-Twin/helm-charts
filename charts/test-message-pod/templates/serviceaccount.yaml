{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ include "test-message-pod.serviceAccountName" . }}
  labels:
    {{- include "test-message-pod.labels" . | nindent 4 }}
{{- end }}
